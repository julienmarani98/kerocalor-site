# Copiare in .env sul server (VPS: /docker/kerocalor-site/.env) e valorizzare.
# Senza queste variabili il pannello admin rifiuta ogni login: nessun default.
SESSION_SECRET=   # stringa random lunga, es. `openssl rand -hex 32`
ADMIN_PASSWORD=   # password del pannello /admin
